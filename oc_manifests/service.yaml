kind: Service
apiVersion: v1
metadata:
  name: observai-query-backend
  namespace: testing-workspace
spec:
  selector:
    app: observai-query-backend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
